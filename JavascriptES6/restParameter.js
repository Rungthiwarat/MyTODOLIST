summation = (...numbers) => {
  let totalsummation = 0;
  for (number of numbers) {
    totalsummation += number;
  }
  return totalsummation;
};
console.log(summation(10, 20, 30));
console.log(summation(10, 20, 30, 50, 60, 70));
